name: OWASP ZAP Security Scan

on:
  pull_request:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      target_url:
        description: 'Target URL to scan'
        required: true
        default: 'http://localhost:3000'

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  zap-scan:
    runs-on: ubuntu-latest
    env:
      TARGET_URL: ${{ inputs.target_url || 'http://localhost:3000' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22.14.0'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Start application
        run: |
          npm run build
          npm start &
          sleep 30  # Wait for app to start
          
      - name: Check if application is running
        run: |
          curl -f ${{ env.TARGET_URL }}/health || echo "Application not responding, continuing with scan anyway"

      - name: Run OWASP ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.8.0
        with:
          target: ${{ env.TARGET_URL }}
          rules_file_name: '.zap/rules.tsv'
          cmd_options: '-a'
          docker_image: 'ghcr.io/zaproxy/zaproxy:stable'
        continue-on-error: true

      - name: Run OWASP ZAP Full Scan
        uses: zaproxy/action-full-scan@v0.5.0
        with:
          target: ${{ env.TARGET_URL }}
          rules_file_name: '.zap/rules.tsv'
          cmd_options: '-a'
          docker_image: 'ghcr.io/zaproxy/zaproxy:stable'
        continue-on-error: true

      - name: Generate ZAP Report
        run: |
          mkdir -p reports
          # ZAP reports are automatically generated by the action
          cp zap_report.html reports/ 2>/dev/null || echo "ZAP report not found"
          cp zap_report.json reports/ 2>/dev/null || echo "ZAP JSON report not found"

      - name: Upload ZAP Reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: zap-reports-${{ github.run_number }}
          path: reports/
          retention-days: 30

      - name: Comment on PR with ZAP Results
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const status = '${{ job.status }}';
            const commentBody = `## 🛡️ OWASP ZAP Security Scan Results

            ### 🔍 Scan Details:
            - **Target**: ${{ env.TARGET_URL }}
            - **Scan Type**: Baseline + Full Scan
            - **Status**: ${status}

            ### 📊 Results:
            - ZAP security reports are available in the workflow artifacts
            - Download and review the HTML/JSON reports
            - Fix any high/critical severity issues before merging

            ### 🚨 Common Issues to Check:
            - Missing security headers
            - XSS vulnerabilities
            - SQL injection points
            - Authentication bypasses
            - Information disclosure

            ---
            *This comment was automatically generated by the OWASP ZAP security scan*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: commentBody
            });
